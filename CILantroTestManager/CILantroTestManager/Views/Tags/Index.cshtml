@model CILantroTestManager.ViewModels.Tags.TagsIndexViewModel

<h2>
    Manage tags
    <a href="@Url.Action("Add", "Tags")" type="button" class="btn btn-success">
        <span class="glyphicon glyphicon-plus"></span>
        Add
    </a>
</h2>

@if(Model.Tags.Count() > 0)
{

}
else
{
    <text>No tags could be found.</text>
}